name: inference-worker
runtime:
  language: python
  package_manager: pip
  entry: inference_worker.py

scripts:
  install: "pip install -r requirements.txt"
  start: "python inference_worker.py"

resources:
  memory: 8192   # MiB — needed for GGUF model weights + PyTorch
  cpus: 4
